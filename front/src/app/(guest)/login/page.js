'use client'

import LoginForm from "../../LoginForm";
import * as S from "./style";

export const LoginPage = () => {
  return (
    <S.LoginWrapper>
      <LoginForm />
    </S.LoginWrapper>
  );
};

export default LoginPage;